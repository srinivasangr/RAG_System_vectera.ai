.git
.github
.venv
**/.venv
**/__pycache__
**/*.pyc
**/.cache
**/.streamlit/secrets.toml
**/.env
**/.env.local
**/uploads
*.log

# The 10 source PDFs are ~50 MB. They're copied in by default — comment the
# next line out if you want them in the image (the hosted demo doesn't need
# them, since Snowflake holds the ingested chunks).
# Documents/
